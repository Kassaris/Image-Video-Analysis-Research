# Data

The notebook uses a reproducible subset of CIFAR-100 through the existing TensorFlow/Keras data-loading workflow.

Dataset files are intentionally not committed to this repository. CIFAR-100 can be obtained through the corresponding TensorFlow/Keras dataset utilities. Any cached datasets, trained weights, or generated artifacts should remain local and follow the original dataset/model licensing terms.
